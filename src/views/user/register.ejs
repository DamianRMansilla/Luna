<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head")%>

<%- include("../partials/header")%>
<body>

    <main>
        <h2 class="register-title">Crea tu cuenta</h2>

        <form class="register" action="/user/register" method="POST" enctype="multipart/form-data">

            <div class="name_last">
                <div class="name">
                    <label for="name">Nombre</label>
                    <input type="text" name="name" id="name" value="<%= locals.old && old.name ? old.name: ''%>">
                    <% if (locals.errors && errors.name) { %>
                        <div class="errores"><%= errors.name.msg %></div>
                    <% } %>
                </div>
                <div class="last_name">
                    <label for="last_name">Apellido</label>
                    <input type="text" name="last_name" id="last_name" value="<%= locals.old && old.last_name ? old.last_name: '' %>">
                    <% if (locals.errors && errors.last_name) { %>
                        <div class="errores"><%= errors.last_name.msg %></div>
                    <% } %>
                </div>
            </div>
            <div>
                <label for="user">Nombre de usuario</label>
                <input type="text" name="user" id="user" value=" <%= locals.old && old.user ? old.user : '' %>">
                <% if (locals.errors && errors.user) { %>
                    <div class="errores"><%= errors.user.msg %></div>
                <% } %>
            </div>
            <div>
                <label for="image">Imagen de usuario</label>
                <input type="file" name="userImage" id="image">
                <% if (locals.errors && errors.userImage) { %>
                    <div class="errores"><%= errors.userImage.msg %></div>
                <% } %>
            </div>
            <div>
                <label for="mail">Correo electrónico</label>
                <input type="mail" name="mail" id="mail" value="<%= locals.old && old.mail ? old.mail : '' %>">
                <% if (locals.errors && errors.mail) { %>
                    <div class="errores"><%= errors.mail.msg %></div>
                <% } %>
            </div>
            <div>
                <label for="password">Contraseña</label>
                <input type="password" name="password" id="password" placeholder="Al menos 8 caracteres">
                <% if (locals.errors && errors.password) { %>
                    <div class="errores"><%= errors.password.msg %></div>
                <% } %>
            </div>
            <p class="terminos">Al pulsar Continuar, confirmas haber leído
                 y aceptado nuestras <a class="condiciones" href="" target="_blank">Condiciones de uso</a>. Para más información 
                 sobre el tratamiento de tus datos, consulta nuestro <a class="privacidad" href="" target="_blank">Aviso de privacidad</a>.</p>
            <div class="robot">
                <input type="checkbox" name="robot" id="robot" required>
                <label for="robot">No soy un robot</label>
            </div>
            <div class="buttons">
                <button type="submit" class="button1">Continuar</button>
                <button type="reset" class="button2">Borrar</button>
            </div>


        </form>
    </main>

<%- include("../partials/footer")%>

</body>
</html>